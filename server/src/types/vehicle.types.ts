export enum VehicleCategory {
  CYCLE = "cycle",
  EV_BIKE = "ev_bike",
  BIKE = "bike",
}

export enum VehicleStatus {
  AVAILABLE = "available",
  RENTED = "rented",
  MAINTENANCE = "maintenance",
  INACTIVE = "inactive",
}